using Microsoft.AspNetCore.SignalR;
using ThreatPilot.Backend.Models;

namespace ThreatPilot.Backend.Hubs
{
    public class AlertHub : Hub
    {
        // Clients can connect to this hub to receive real-time alerts.
        // We can add methods here if the client needs to send data to the server via SignalR.
    }
}
